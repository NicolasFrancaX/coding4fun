#include <bits/stdc++.h>

#define debug(x) cout << #x " = " << endl;

using namespace std;

int main() {
    int A, B;

    while (cin >> A >> B && A+B) {
        cout << A+B << endl;
    }

    return 0;
}